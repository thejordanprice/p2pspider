html
    include includes/header.pug
    body
        include includes/navbar.pug
        .container
            h4="Database: " + statistics.db
            h5="Collections: " + statistics.collections
            h5="Objects: " + statistics.objects
            h5="Avg. Size: " + statistics.avgObjSize + " MB"
            h5="Data Size: " + statistics.dataSize + " MB"
            h5="Storage Size: " + statistics.storageSize + " MB"
            h5="Indexes: " + statistics.indexes
            h5="Index Size: " + statistics.indexSize + " MB"
            h5="Filesystem Used: " + statistics.fsUsedSize + " MB"
            h5="Filesystem Total: " + statistics.fsTotalSize + " MB"