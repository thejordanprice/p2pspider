<head>
  <title><%= typeof title !== 'undefined' ? title : 'P2P Magnet Search' %></title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="google" value="notranslate">
  <meta name="description" content="A searchable database of magnet URIs from the DHT swarm">
  <meta name="theme-color" content="#0ea5e9">
  <meta name="color-scheme" content="light">

  <!-- Preload critical assets -->
  <link rel="preload" href="public/css/tailwind.css" as="style">

  <!-- Prevent flash of unstyled content -->
  <style>
    :root {
      --primary: #0ea5e9;
      --primary-dark: #0284c7;
      --secondary: #7c3aed;
      --dark-bg: #1e293b;
      --card-shadow: rgba(0, 0, 0, 0.1) 0px 10px 15px -3px, rgba(0, 0, 0, 0.05) 0px 4px 6px -2px;
    }
    .search-form button { opacity: 0; }
    .search-form button i { visibility: hidden; }
    body.ready .search-form button { opacity: 1; }
    body.ready .search-form button i { visibility: visible; }
    body { opacity: 0.98; transition: opacity 0.2s ease-in-out; }
    body.ready { opacity: 1; }
    @keyframes fadeIn { 0% { opacity: 0; } 100% { opacity: 1; } }
    .animate-fadeIn { animation: fadeIn 0.5s ease-in-out; }
    .btn-primary {
      @apply bg-primary-500 hover:bg-primary-600 text-white font-medium py-2 px-4 rounded-lg transition-all duration-200;
    }
    .btn-secondary {
      @apply bg-dark-700 hover:bg-dark-800 text-white font-medium py-2 px-4 rounded-lg transition-all duration-200;
    }
    .shadow-elegant {
      box-shadow: rgba(50, 50, 93, 0.1) 0px 13px 27px -5px, rgba(0, 0, 0, 0.15) 0px 8px 16px -8px;
    }
    .shadow-elegant-lg {
      box-shadow: rgba(50, 50, 93, 0.25) 0px 13px 27px -5px, rgba(0, 0, 0, 0.3) 0px 8px 16px -8px;
    }
  </style>

  <!-- Stylesheets -->
  <link rel="stylesheet" href="public/css/tailwind.css">
  <link rel="stylesheet" href="public/static/css/fontawesome/all.min.css">
  <link rel="stylesheet" href="public/static/css/google-fonts.css">
  
  <!-- Favicons -->
  <link rel="icon" href="public/favicon/favicon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" sizes="180x180" href="public/favicon/apple-touch-icon.png">
  <link rel="manifest" href="public/favicon/site.webmanifest">
  
  <!-- Scripts -->
  <script src="public/static/js/alpine.min.js" defer></script>
</head> 