html
    include includes/header.pug
    body
        include includes/navbar.pug
        .container
            each item in result
                .card
                    .card-header
                        h2= item.name
                    .card-body
                        p.card-text= 'Infohash: ' + item.infohash
                        p.card-text= 'Fetched At: ' + new Date(item.fetchedAt)
                        pre= item.toObject().files || 'files: unknown atm...'
                        a.btn.btn-default.card-link(href=item.magnet) Magnet
            br
            a(href='/') Back

            br
            br
            include includes/footer.pug
